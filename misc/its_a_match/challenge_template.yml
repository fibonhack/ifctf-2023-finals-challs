name: "It's a match!"
author: "gio-d3141"

description: "Regular languages are not Turing complete, but apparently they can do maths!!?!\n\nNote: the pow for this challenge should be solvable in around 30 seconds of computation. Please, test locally your solution before submitting it to the server."
port: 10011
host: 'challs.{{ CTFD_URL }}'
connection_info: 'nc {{ host }} {{ port }}'

healthcheck: 'solve/healthcheck.py --hostname={{ host }} --port={{ port }}'

topics:
- misc

files:
- src/server.py
